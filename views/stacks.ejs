<!DOCTYPE html>
<html lang="en">
    <%- include('./partials/head') %>
    <style>
                h1 {
            display: block;
            font-size: 2em;
            margin-block-start: 0.67em;
            margin-block-end: 0.67em;
            margin-inline-start: 0px;
            margin-inline-end: 0px;
            font-weight: bold;
            unicode-bidi: isolate;
        }
        p {
            color: #ccc;
            filter: blur(0.30px);
        }
        body {
            color-scheme: dark;
        }
    </style>
<body class="bg-black">  
    <div class="container" style="display: flex; margin-left: 0; margin-right: auto;">
        <%- include('./partials/sidebar') %>
        <div class="content p-5">
            <h1 class="text-white">Stacks</h1>
            <p>Here, you can find the stacks that I used to make this website.</p>
            <div class="stacks">
                <!--stack card-->
                <% stacks.forEach(stack => { %>
                    <%- include('./partials/stack-card',stack) %>
                <% }) %>
            </div>
        </div>
    </div>  
</body>
</html>
